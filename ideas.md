next step:


add a utility that copies the needed papers and checkpoint files to a folder (default this:
DATA_DEFAULT=P:\24-0012_KTS_RSE-Master\05_Research\lni_study_working_files) so the second coder can proceed coding after having topped
off the goldstandard pool. This assumes the second coder makes a pull request to get the current version of coding_username.csv via git. 
Also ensure that the changes are reflected in all coding_username.csv files (keeping backups) and in sync with each other
(requires git pull by user), also prompt the current user to run this utiliy after the gold-topping off step 